import SwiftUI

public struct SheetHandle: View {
    public init() {}

    public var body: some View {
        Capsule()
            .fill(AppColor.border2)
            .frame(width: 36, height: 4)
    }
}
